---
title: Props / Callback 与 props / emit
description: React 通过 props 传值并用回调向上传递事件；Vue 使用 props 与 emit 表达同样的数据流。
---

import Comparison from "./Comparison.astro";

同一个“子组件输入消息、父组件接收结果”的 Demo。重点看 React 如何把函数当作 props 传入，以及 Vue 如何声明并触发组件事件。

<Comparison />
