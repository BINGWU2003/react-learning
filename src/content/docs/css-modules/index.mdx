---
title: CSS Modules 与 scoped
description: CSS Modules 通过生成后的类名隔离样式；Vue scoped 为组件模板和选择器添加作用域属性。
---

import Comparison from "./Comparison.astro";

两个卡片使用相同结构与视觉效果。React 显式导入样式对象，Vue 则在单文件组件中直接使用普通类名。

<Comparison />
