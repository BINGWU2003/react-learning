---
title: 派生状态与 computed
description: React 在渲染期间从现有状态计算结果；Vue 使用 computed 声明缓存的派生值。
---

import Comparison from "./Comparison.astro";

输入搜索词并切换“仅显示有库存”，两个 Demo 都从原始商品与筛选条件推导可见列表，不额外同步一份列表状态。

<Comparison />
