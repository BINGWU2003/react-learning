---
title: Props 动态样式与 CSS 变量
description: 两边都可以把运行时数据映射到 CSS 自定义属性，差异集中在状态与 style 绑定语法。
---

import Comparison from "./Comparison.astro";

切换主题会同时改变强调色、背景与圆角。Demo 的 CSS 结构一致，只对比如何从组件状态生成自定义属性。

<Comparison />
