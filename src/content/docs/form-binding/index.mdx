---
title: 受控表单与 v-model
description: React 显式连接 value 和事件处理函数；Vue 使用 v-model 描述表单值的双向绑定。
---

import Comparison from "./Comparison.astro";

两个表单都实时展示姓名和方向。React 把输入值保持在 state 中，Vue 通过 `v-model` 把表单控件与 ref 连接起来。

<Comparison />

