---
title: useState 与 ref / reactive
description: React 使用 setter 提交新状态；Vue 通过 ref 和 reactive 对响应式数据进行追踪。
---

import Comparison from "./Comparison.astro";

两个计数器维护相同的数字和对象状态。操作结果一致，更新状态时的写法与心智模型不同。

<Comparison />
