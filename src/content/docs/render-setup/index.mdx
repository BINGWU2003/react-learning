---
title: React render 与 Vue setup
description: React 函数组件会随状态更新重新执行；Vue setup 通常只在组件创建时执行一次。
---

import Comparison from "./Comparison.astro";

两个 Demo 都维护计数和显示模式，并记录组件逻辑的执行次数。操作结果相同，但状态更新后重新工作的范围不同。

<Comparison />

